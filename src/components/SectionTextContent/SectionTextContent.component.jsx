import React from 'react';
import classes from './SectionTextContent.module.scss';

const SectionTextContent = () => {
  return (
    <div className={classes}>
    </div>
  );
};

export default SectionTextContent;